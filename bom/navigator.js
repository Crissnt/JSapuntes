//Returns a string with browser info

navigator.useragent

//Returns an object to manage from the clipboard

navigator.clipboard

//Returns a boolean which tells if cookies are enabled

navigator.cookieEnabled

//Returns an object which allows to use the geolocation API to determine the user's location

navigator.geolocation

//Returns a boolean which tells if the Java plugin is enabled in the browser

navigator.javaEnabled()

//Returns a string with the browser's language code

navigator.language

//Returns an array with MIME types accepted by the browser

navigator.mimeTypes

//Returns a boolean. Tells if the browser is working online or not

navigator.onLine

//Returns an array with information about the plugins installed in the browser

navigator.plugins

//Returns a ServiceWorkerContainer type object

navigator.serviceWorker

//Returns a StorageManager type object able to manage the storage API of persistant objects

navigator.storage
